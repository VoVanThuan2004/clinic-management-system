export type CategoryResponse = {
  categoryId: string;
  categoryName: string;
  createdAt: string;
  updatedAt: string;
};

export type CategoryOption = {
  categoryId: string;
  categoryName: string;
};
