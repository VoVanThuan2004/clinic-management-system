export type Category = {
  category_id: string;
  category_name: string;
  created_at: string;
  updated_at: string;
};

export type CategoryOption = {
  categoryId: string;
  categoryName: string;
};
